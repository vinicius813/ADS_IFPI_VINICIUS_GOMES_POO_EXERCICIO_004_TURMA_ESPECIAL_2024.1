class Hotel {
    quantReservas : number = 0;  // Inicializando a variável quantReservas

    adicionarReserva() : void {
        this.quantReservas++;  // Incrementando o número de reservas.
    }
}